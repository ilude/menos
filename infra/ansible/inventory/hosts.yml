all:
  hosts:
    menos:
      ansible_host: "${MENOS_HOST}"
      ansible_user: "${MENOS_USER:-root}"
      ansible_ssh_private_key_file: "${SSH_KEY_PATH:-~/.ssh/id_ed25519}"
  vars:
    deploy_path: /apps/menos
    data_path: /apps/menos/data
